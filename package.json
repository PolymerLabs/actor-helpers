{
  "name": "westend-helpers",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "tsc",
    "clean": "touch lib && rm -rf lib && mkdir lib",
    "test": "npm run clean && npm run build && karma start"
  },
  "author": "",
  "license": "BSD-3-Clause",
  "dependencies": {
    "typescript": "^3.0.3"
  },
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.5",
    "chai": "^4.1.2",
    "karma": "^3.0.0",
    "karma-chai": "^0.1.0",
    "karma-chrome-launcher": "^2.2.0",
    "karma-mocha": "^1.3.0",
    "mocha": "^5.2.0"
  }
}
